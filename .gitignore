__pycache__/
*.pyc
.venv/
venv/

# Crawl output: raw HTML, spreadsheets, RAG corpus. Regenerate, don't commit.
data/
demo_data/
*.xlsx
*.csv

.DS_Store

# Jobber OAuth tokens. These live outside the repo by default; this is here in
# case JOBBER_TOKEN_FILE is ever pointed inside it.
jobber_token.json
*.token.json
.env
