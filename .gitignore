__pycache__/
*.pyc
.venv/
venv/
.DS_Store
demo_data/

# 135 MB of saved page HTML: a re-run cache, not a deliverable.
# Regenerate with `python -m mvh run`.
data/raw/

# Redundant local archives (the contents are tracked above).
/*.zip
